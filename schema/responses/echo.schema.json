{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Echo Response",
    "type": "object",
    "properties": {
        "data": {
            "description": "Same as the data in the request, may be empty when it does not exist in request"
        }
    },
    "additionalProperties": false
}