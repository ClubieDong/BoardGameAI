{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "ActionGenerator types",
    "description": "Action generator type",
    "type": "string",
    "anyOf": [
        {
            "description": "Action generators of tic-tac-toe",
            "enum": [
                "default"
            ]
        },
        {
            "description": "Action generators of gobang",
            "enum": [
                "default",
                "neighbor"
            ]
        }
    ]
}