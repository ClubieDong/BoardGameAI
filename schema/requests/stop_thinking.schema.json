{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "StopThinking Request",
    "description": "Let the specified player stop thinking",
    "type": "object",
    "properties": {
        "gameID": {
            "description": "The ID of the game to which the state belongs",
            "$ref": "basic/id.schema.json"
        },
        "stateID": {
            "description": "The ID of the state to which the player belongs",
            "$ref": "basic/id.schema.json"
        },
        "playerID": {
            "description": "The ID of the player to stop thinking",
            "$ref": "basic/id.schema.json"
        }
    },
    "required": [
        "gameID",
        "stateID",
        "playerID"
    ],
    "additionalProperties": false
}